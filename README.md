# linux-in-windows

I'm just going to get right to the point, if you want to run linux in windows (10 or 11) 
you can use an emulator, or dual boot buuuuuut we're going do it via cmd (command line)

first: press the windows key and type cmd and press enter

second copy and past this: wsl --install
